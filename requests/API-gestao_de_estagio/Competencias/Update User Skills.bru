meta {
  name: Update User Skills
  type: http
  seq: 3
}

put {
  url: {{HTTP}}{{DOMAIN}}/{{API_PATH}}/skill/user/6
  body: json
  auth: inherit
}

body:json {
  {
    "skillIds": [
      
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
